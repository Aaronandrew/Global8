import 'package:flutter/material.dart';

// Dark Mode Color Palette
const startBackgroundColorDark = Color.fromRGBO(0, 3, 64, 0.8745098039215686);
const mobileBackgroundColorDark = Color.fromRGBO(0, 3, 64, 0.8745098039215686);
const webBackgroundColorDark = Color.fromRGBO(28, 0, 182, 0.34901960784313724);
const mobileSearchColorDark = Color.fromRGBO(203, 53, 53, 1.0);
const blueColorDark = Color.fromRGBO(239, 139, 255, 0.9803921568627451);
const primaryColorDark = Colors.purpleAccent;
const secondaryColorDark = Colors.white;
const appBarColorDark = Color(0xA0301E77);
const navBarColorDark = Color(0xA3000340);
const textColorDark = Color.fromRGBO(255, 255, 255, 1.0);
const iconColorDark = Color.fromRGBO(102, 80, 236, 1.0);

// Light Mode Color Palette (Purple Theme)
const startBackgroundColorLight = Color(0xFFF3E5F5); // Light lavender/purple tint for start background
const mobileBackgroundColorLight = Color(0xFFF3E5F5); // Same as start background for consistency
const webBackgroundColorLight = Color(0xFFE1BEE7); // Slightly deeper shade of lavender for web background
const mobileSearchColorLight = Color(0xFFBA68C8); // Vibrant purple for search input
const blueColorLight = Color(0xFF9575CD); // Muted purple-blue for accents
const primaryColorLight = Colors.purple; // Primary color for key UI elements
const secondaryColorLight = Colors.white; // Clean white for contrast and secondary elements
const appBarColorLight = Color(0xFFF3F3F3); // Deep purple for app bar
const navBarColorLight = Color(0xFFB6B6B6); // Same as app bar for consistency
const textColorLight = Colors.black; // Black text for high contrast on light backgrounds
const iconColorLight = Colors.white;